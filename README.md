# 📐 Geometric Library

<div align="center">

![Python Version](https://img.shields.io/badge/python-3.8%2B-blue)
![Tests](https://img.shields.io/badge/tests-33%20passed-brightgreen)
![Coverage](https://img.shields.io/badge/coverage-100%25-success)
![License](https://img.shields.io/badge/license-MIT-yellow)

**Библиотека для вычисления геометрических параметров фигур**

</div>

---

## 📋 Содержание
- [✨ Особенности](#✨-особенности)
- [📚 API Документация](#📚-api-документация)
- [🧪 Тестирование](#🧪-тестирование)
- [🔧 Использование](#🔧-использование)

---

## ✨ Особенности

- **4 геометрические фигуры**: круг, прямоугольник, квадрат, треугольник
- **Простой и понятный API**: каждая функция делает ровно одну вещь
- **Полное покрытие тестами**: 33 unit-теста для проверки корректности
- **Подробная документация**: каждая функция имеет docstring с примерами
- **Без зависимостей**: работает на чистом Python

---

## 📚 API Документация

### 🔵 Круг (circle.py)

#### `area(r)`
**Описание:** Вычисляет площадь круга  
**Параметры:**
- `r` (float): радиус круга
**Возвращает:** площадь круга (float)  
**Формула:** π × r²  
**Пример:**
```python
from circle import area
area(5)  # Возвращает 78.53981633974483
```
#### `perimeter(r)`
**Описание:** Вычисляет длину окружности  
**Параметры:**
- `r` (float): радиус окружности
**Возвращает:** длина окружности (float)  
**Формула:** 2 × π × r  
**Пример:**
```python
from circle import perimeter
perimeter(5)  # Возвращает 31.41592653589793
```
### 🟦 Прямоугольник (rectangle.py)

#### `area(a, b)`
**Описание:** Вычисляет площадь прямоугольника  
**Параметры:**
- `a` (float): длина первой стороны
- `b` (float): длина второй стороны (смежной)
**Возвращает:** площадь прямоугольника (float)  
**Формула:** a × b  
**Пример:**
```python
from rectangle import area
area(4, 5)  # Возвращает 20
```
#### `perimeter(a, b)`
**Описание:** Вычисляет периметр прямоугольника  
**Параметры:**
- `a` (float): длина первой стороны
- `b` (float): длина второй стороны (смежной)
**Возвращает:** периметр прямоугольника (float)  
**Формула:** 2 × (a + b)  
**Пример вызова:**
```python
from rectangle import perimeter
perimeter(4, 5)  # Возвращает 18
```

### 🟥 Квадрат (square.py)

#### `area(a)`
**Описание:** Вычисляет площадь квадрата  
**Параметры:**
- `a` (float): длина стороны квадрата
**Возвращает:** площадь квадрата (float)  
**Формула:** a²  
**Пример вызова:**
```python
from square import area
area(4)  # Возвращает 16
```


#### `perimeter(a)`
**Описание:** Вычисляет периметр квадрата  
**Параметры:**
- `a` (float): длина стороны квадрата
**Возвращает:** периметр квадрата (float)  
**Формула:** 4 × a  
**Пример вызова:**
```python
from square import perimeter
perimeter(4)  # Возвращает 16
```


### 🔺 Треугольник (triangle.py)

#### `area(a, h)`
**Описание:** Вычисляет площадь треугольника  
**Параметры:**
- `a` (float): длина основания треугольника
- `h` (float): высота треугольника, проведенная к основанию
**Возвращает:** площадь треугольника (float)  
**Формула:** (a × h) ÷ 2  
**Пример вызова:**
```python
from triangle import area
area(6, 4)  # Возвращает 12.0
```


#### `perimeter(a, b, c)`
**Описание:** Вычисляет периметр треугольника  
**Параметры:**
- `a` (float): длина первой стороны
- `b` (float): длина второй стороны
- `c` (float): длина третьей стороны
**Возвращает:** периметр треугольника (float)  
**Формула:** a + b + c  
**Пример вызова:**
```python
from triangle import perimeter
perimeter(3, 4, 5)  # Возвращает 12
```
## 🧪 Тестирование

### Запуск тестов
```bash
# Запустить все тесты
python -m unittest discover -v

# Запустить тесты для конкретного модуля
python -m unittest test_circle.py -v
python -m unittest test_rectangle.py -v
python -m unittest test_square.py -v
python -m unittest test_triangle.py -v

# Использовать скрипт для запуска
python run_tests.py
```
## Что проверяют тесты?
**✅ Нормальные случаи - правильные вычисления с корректными данными**  
**✅ Граничные случаи - работа с нулевыми значениями**  
**✅ Математическая точность - сравнение с ожидаемыми значениями**  
**✅ Особые случаи - обработка различных входных данных**  

## 🔧 Использование

### Основные сценарии использования

#### Сценарий 1: Образовательные цели
Библиотека идеально подходит для обучения основам программирования и математики:
- Изучение работы с функциями в Python
- Понимание математических формул
- Практика импорта модулей
- Работа с числами с плавающей точкой

#### Сценарий 2: Быстрые расчеты
Использование для быстрых геометрических вычислений:
- Расчет площадей помещений
- Определение периметров ограждений
- Вычисление материалов для строительства
- Проектирование ландшафтных объектов

#### Сценарий 3: Интеграция в другие проекты
Библиотека может быть использована как зависимость в более сложных проектах:
- Графические приложения
- Инженерные расчеты
- Образовательные программы
- Тестовые задания для студентовv